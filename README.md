Mới chỉnh xong khung giao diện "cứng":

cd NumbersGame

mvn -pl client compile exec:java "-Dexec.mainClass=com.numbergame.client.MainClient"

Chạy rồi bấm Đăng nhập (LoginFrame) -> Vào phòng (LobbyFrame) -> Trò chơi (GamePanel)